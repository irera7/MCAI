<Page x:Class="ModelCreator.UI.Views.CreateProjectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="Create New Project">

    <Grid Background="{StaticResource BackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40" MaxWidth="800">
                
                <TextBlock Text="Create New Project" 
                          FontSize="32" 
                          FontWeight="Bold"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          Margin="0,0,0,30"/>
                
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <!-- Project Name -->
                        <TextBlock Text="Project Name" 
                                  FontSize="16" 
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  Margin="0,0,0,10"/>
                        <TextBox x:Name="ProjectNameTextBox" 
                                Height="40" 
                                FontSize="14"
                                Margin="0,0,0,20"/>
                        
                        <!-- Description -->
                        <TextBlock Text="Description (Optional)" 
                                  FontSize="16" 
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  Margin="0,0,0,10"/>
                        <TextBox x:Name="DescriptionTextBox" 
                                Height="80" 
                                FontSize="14"
                                TextWrapping="Wrap"
                                AcceptsReturn="True"
                                VerticalScrollBarVisibility="Auto"
                                Margin="0,0,0,20"/>
                        
                        <!-- Data Type Selection -->
                        <TextBlock Text="Select Data Type" 
                                  FontSize="16" 
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  Margin="0,0,0,10"/>
                        
                        <ComboBox x:Name="ModalityComboBox" 
                                 Height="40" 
                                 FontSize="14"
                                 Margin="0,0,0,30">
                            <ComboBoxItem Content="Image Classification" Tag="image" IsSelected="True"/>
                            <ComboBoxItem Content="Text Classification" Tag="text"/>
                            <ComboBoxItem Content="Audio Classification" Tag="audio"/>
                            <ComboBoxItem Content="Video Classification" Tag="video"/>
                            <ComboBoxItem Content="Tabular Data" Tag="tabular"/>
                            <ComboBoxItem Content="Time Series" Tag="timeseries"/>
                            <ComboBoxItem Content="Medical Data" Tag="medical"/>
                            <ComboBoxItem Content="Genomic Data" Tag="genomic"/>
                        </ComboBox>
                        
                        <!-- Action Buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Cancel" 
                                   Width="120" 
                                   Height="40"
                                   Margin="0,0,10,0"
                                   Click="Cancel_Click"/>
                            <Button Content="Create Project" 
                                   Style="{StaticResource PrimaryButton}"
                                   Width="150" 
                                   Height="40"
                                   Click="CreateProject_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>

