<Page x:Class="ModelCreator.UI.Views.ProjectsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="Projects">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Padding="40,30,40,20">
            <Grid>
                <TextBlock Text="Your Projects" 
                          FontSize="32" 
                          FontWeight="Bold"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          VerticalAlignment="Center"/>
                <Button Content="+ New Project" 
                       Style="{StaticResource PrimaryButton}"
                       HorizontalAlignment="Right"
                       Click="NewProject_Click"/>
            </Grid>
        </Border>
        
        <!-- Projects List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ProjectsList" Margin="40,0,40,40">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Cursor="Hand" MouseLeftButtonDown="Project_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" 
                                              FontSize="20" 
                                              FontWeight="SemiBold"
                                              Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <TextBlock Text="{Binding Description}" 
                                              FontSize="14" 
                                              Foreground="{StaticResource TextSecondaryBrush}"
                                              Margin="0,5,0,0"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <TextBlock Text="Type: " 
                                                  FontSize="12" 
                                                  Foreground="{StaticResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{Binding Modality}" 
                                                  FontSize="12" 
                                                  FontWeight="SemiBold"
                                                  Foreground="{StaticResource PrimaryBrush}"/>
                                        <TextBlock Text=" | " 
                                                  FontSize="12" 
                                                  Foreground="{StaticResource TextSecondaryBrush}"
                                                  Margin="10,0,10,0"/>
                                        <TextBlock Text="{Binding DisplayStatus}" 
                                                  FontSize="12" 
                                                  FontWeight="SemiBold"
                                                  Foreground="{StaticResource AccentBrush}"/>
                                    </StackPanel>
                                    
                                    <!-- Training progress bar (only visible when training) -->
                                    <ProgressBar Value="{Binding TrainingProgress}" 
                                                Visibility="{Binding IsTraining, Converter={StaticResource BoolToVisibilityConverter}}"
                                                Height="4"
                                                Margin="0,8,0,0"
                                                Background="#E0E0E0"
                                                Foreground="{StaticResource PrimaryBrush}"/>
                                </StackPanel>
                                
                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                                    <!-- Playground Button (only visible if trained) -->
                                    <Button Content="🎮 Playground" 
                                           Width="120"
                                           Height="35"
                                           Margin="0,0,10,0"
                                           Style="{StaticResource PrimaryButton}"
                                           Visibility="{Binding IsTrained, Converter={StaticResource BoolToVisibilityConverter}}"
                                           Click="Playground_Click"/>
                                    
                                    <Button Content="Delete" 
                                           Width="80"
                                           Height="35"
                                           Click="DeleteProject_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Loading/Empty State -->
        <TextBlock x:Name="EmptyStateText"
                  Grid.Row="1"
                  Text="No projects yet. Create your first project!"
                  FontSize="18"
                  Foreground="{StaticResource TextSecondaryBrush}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Visibility="Collapsed"/>
    </Grid>
</Page>

